<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.multi_reminder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".custom" android:label="Custom reminder" android:icon="@drawable/ic_settings_blackdp"/>
        <activity android:name=".exercise" android:label="Workout" android:icon="@drawable/ic_workout"/>
        <activity android:name=".cleaning" android:label="Clean" android:icon="@drawable/ic_clean"/>
        <activity android:name=".hands_daily" android:label="Wash hands" android:icon="@drawable/ic_hand_wash"/>
        <activity android:name=".eat_daily" android:label="Eat" android:icon="@drawable/ic_eat"/>
        <activity android:name=".drink_daily" android:label="Drink Water" android:icon="@drawable/ic_water_glass"/>
        <activity android:name=".MainActivity" android:label="Get your S#!T together">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>